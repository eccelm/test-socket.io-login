import React from "react";
/*
   Also use a ternary
   If props.username.length > 0  ==> show change username func // else login-in page
*/
const Login = () => {
  return <div className="Login"></div>;
};

export default Login;
